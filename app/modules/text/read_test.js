'use strict';

describe('lodBook.read module', function() {

  beforeEach(module('lodBook.read'));

  describe('read controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var readCtrl = $controller('ReadCtrl');
      expect(readCtrl).toBeDefined();
    }));

  });
});